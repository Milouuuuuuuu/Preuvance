# Comportement de construction — Preuvance

Dernière mise à jour : 14 juillet 2026.

Ce document est la mémoire de décision du projet. Toute décision produit, réglementaire, IA, données, sécurité ou architecture qui modifie le comportement livré doit être ajoutée au registre avec une note sur 100.

## Barème

- **90–100** : décision verrouillée, étayée par une source primaire, un test ou une contrainte explicite.
- **75–89** : décision forte, révisable si une meilleure preuve apparaît.
- **60–74** : hypothèse de travail ; elle ne doit pas devenir une promesse publique sans validation.
- **< 60** : expérimentation ; elle reste hors du parcours critique.

La note mesure la solidité de la décision au moment où elle est prise, pas une probabilité juridique ni un score d’assurabilité.

## Règles de comportement

1. **Rechercher avant d’affirmer.** Les noms, domaines, modèles, délais et obligations sont vérifiés sur des sources primaires actuelles.
2. **Séparer le droit contraignant du droit annoncé.** Une mesure adoptée mais non encore entrée en vigueur reste présentée comme un scénario futur.
3. **Ne rien inventer pour rendre une démo plus jolie.** Pas de faux appel API, de résultat simulé présenté comme réel, ni de fallback de modèle silencieux.
4. **Structurer puis calculer.** Le modèle extrait, classe et propose des écarts dans des schémas stricts ; le score final est calculé de façon déterministe et auditable.
5. **Montrer l’incertitude.** Chaque étape expose une note sur 100, ses limites et les informations manquantes.
6. **Ne pas promettre l’assurabilité.** Preuvance prépare un dossier de pré-souscription ; seul un courtier ou assureur décide d’une couverture.
7. **Protéger les données.** Secrets hors dépôt, validation serveur systématique, contrôle d’accès au niveau base de données, aucune donnée client dans les logs applicatifs.
8. **Gater les phases.** Pipeline et tests d’abord ; interface, PDF, persistance puis finition seulement après validation de la phase précédente.
9. **Livrer accessible.** Contrastes AA, navigation clavier, HTML sémantique, messages d’erreur explicites et respect de `prefers-reduced-motion`.
10. **Mettre ce registre à jour.** Une décision matérielle non notée n’est pas considérée comme stabilisée.

## Registre des décisions

| ID | Décision | Note /100 | Justification et conséquence |
|---|---|---:|---|
| D-001 | Remplacer la marque **Aplomb** par **Preuvance** | 86 | Les trois domaines ciblés sont enregistrés et Aplomb Strategies opère déjà en gouvernance IA/réglementaire. Preuvance combine « preuve » et « assurance » et présente de bons signaux RDAP préliminaires. Un achat registrar et une recherche EUIPO/TMview restent nécessaires. |
| D-002 | Positionner le produit comme un **dossier de pré-souscription IA en français pour PME/SMC européennes** | 82 | Le pont assurance existe déjà chez Armilla, Munich Re et Insured AI. Le différenciateur crédible est l’entrée libre en français, la traçabilité réglementaire datée et la préparation courtier pour le segment UE. |
| D-003 | Afficher séparément **droit en vigueur** et **scénario Omnibus** | 100 | Au 13/07/2026, l’Omnibus est adopté et signé mais attend sa publication au JOUE ; il n’a donc pas encore d’effet juridique. |
| D-004 | Ne pas présenter le 02/12/2026 comme date générale de « watermarking » | 100 | Cette transition ne concerne que l’article 50(2) et les systèmes déjà mis sur le marché avant le 02/08/2026. Le terme juridique sûr est « marquage détectable dans un format lisible par machine ». |
| D-005 | Définir une SMC par moins de 750 salariés et **CA ≤ 150 M€ ou bilan ≤ 129 M€**, hors PME | 100 | Le brief omettait le seuil de bilan et le caractère alternatif des seuils financiers. |
| D-006 | Utiliser `gpt-5.6-sol` pour les décisions importantes | 99 | Modèle officiel actuel recommandé pour le raisonnement complexe et les sorties structurées. |
| D-007 | Réserver `gpt-5.6-luna` aux tâches simples/économiques après évaluation | 96 | Modèle documenté pour les charges sensibles au coût ; aucune substitution silencieuse sur une décision réglementaire. |
| D-008 | Utiliser l’API Responses avec JSON Schema strict à chaque appel | 98 | Tous les champs sont requis, les objets refusent les propriétés additionnelles et les refus/réponses incomplètes sont traités explicitement. |
| D-009 | Calculer le score de préparation de façon déterministe après les appels IA | 97 | Rend le résultat reproductible, explicable et testable ; le LLM ne choisit pas directement la note finale. |
| D-010 | Refuser un lancement sans clé API au lieu de fabriquer un résultat | 100 | Respecte l’interdiction de faux API et évite qu’un exemple soit confondu avec une analyse réelle. |
| D-011 | Produire un PDF de **préparation courtier**, jamais un certificat | 98 | Le rapport assemble preuves, lacunes, questions et limites ; il ne tranche ni couverture, ni prime, ni conformité juridique. |
| D-012 | Utiliser Supabase avec RLS pour comptes, organisations, systèmes et évaluations | 94 | Correspond au périmètre explicite ; chaque accès utilisateur doit être vérifié côté serveur et base. |
| D-013 | Garder Resend désactivé derrière un drapeau tant qu’aucun envoi n’est requis | 95 | Évite un effet externe et un secret supplémentaire dans le MVP. |
| D-014 | Respecter les quatre phases et faire passer compilation/tests à chaque gate | 98 | Réduit le risque de masquer un pipeline défaillant sous une interface polie. |
| D-015 | Conserver le socle Sites/Vinext pour l’hébergement Cloudflare | 92 | Le projet a été initialisé avec le runtime Sites demandé ; l’implémentation reste compatible Worker et évite les API serveur non portables. |
| D-016 | Épingler Next.js `15.5.20` et React `19.2.6` | 97 | Respecte la stack explicitement demandée ; les peer dependencies publiées confirment la compatibilité React 19 et Vinext impose React 19.2.6. |
| D-017 | Charger Yoga comme **WASM statique précompilé** et ne résoudre React côté client que pour `@react-pdf/reconciler` | 96 | Workerd interdit la compilation WASM dynamique et Vinext applique la condition React Server. L’adaptateur ciblé évite un alias React global qui casserait les Server Components ; le test runtime renvoie un vrai PDF signé `%PDF-`. |
| D-018 | Construire dans la synthèse un **contrat de rapport unique**, validé par Zod et partagé par l’UI, le PDF et Supabase | 98 | Élimine les divergences de schéma et empêche le PDF ou la base de recalculer ou d’inventer un résultat. Organisation et système sont obligatoires dans le formulaire afin que le dossier soit nominatif. |
| D-019 | Exiger une session quand Supabase est configuré et persister toute évaluation terminée dans une transaction RPC | 97 | Organisation, système, évaluation, rapport et journal des quatre étapes sont écrits atomiquement sous RLS ; une écriture partielle n’est jamais annoncée comme réussie. |
| D-020 | Laisser Supabase optionnel en développement, sans prétendre à une persistance | 94 | Sans variables Supabase, l’évaluation peut fonctionner avec OpenAI et retourne explicitement `persistence.status = disabled`. Avec Supabase configuré, l’authentification et la persistance deviennent obligatoires. |
| D-021 | Valider le formulaire avec **React Hook Form + Zod** avant la validation serveur | 96 | Les mêmes bornes de longueur et de taille d’entreprise sont contrôlées côté interface, puis de nouveau par le schéma strict de l’API. La validation navigateur ne remplace jamais la validation serveur. |
| D-022 | Gater la livraison sur lint, TypeScript strict, tests unitaires, build et test HTTP du PDF sous Workerd | 99 | Ces contrôles couvrent le référentiel, le score, le contrat PDF, le rendu français et la contrainte d’exécution Cloudflare qui avait révélé les incompatibilités React/WASM. |
| D-023 | Diffuser la progression réelle du pipeline en **NDJSON**, sans chaîne de pensée | 97 | L’interface change d’étape uniquement lorsqu’un événement serveur `extraction`, `classification`, `gap_analysis` ou `synthesis` est reçu. Le flux n’expose que l’état, jamais le raisonnement interne du modèle. |
| D-024 | Refuser tout endpoint OpenAI anonyme en production | 99 | En production, Supabase et une session valide sont obligatoires avant les trois appels modèle. Le mode sans Supabase reste réservé au développement local et n’annonce aucune persistance. |
| D-025 | Ne pas transformer une annexe `insufficient_information` en obligation applicable | 99 | Une qualification à confirmer reste dans le journal des décisions et les informations manquantes ; seules les issues `applies` et `likely_applies` ajoutent les obligations Annexe I/III au dossier. |
| D-026 | Utiliser Supabase comme unique base applicative et retirer le squelette D1/Drizzle inutilisé | 97 | Le brief impose Postgres, Auth et RLS Supabase. Conserver une seconde pile de données inactive aurait créé une ambiguïté de déploiement et une surface de maintenance sans fonctionnalité utilisateur. |
| D-027 | Faire du **E à trois barres** le mot-symbole officiel de Preuvance | 98 | Le signe distinctif existe déjà dans l’image sociale validée par l’utilisateur. Le reconstruire en HTML/CSS le garde net, accessible et cohérent à toutes les tailles sans ajouter d’asset. |
| D-028 | Afficher l’image `og.png` dans un encart éditorial chargé sous la ligne de flottaison | 94 | L’image appréciée devient visible dans le produit avec chargement différé, sans alourdir le LCP du hero ni dupliquer son texte pour les lecteurs d’écran. |
| D-029 | Limiter le mouvement aux entrées, interactions et **vrais changements d’état serveur** | 96 | Le hero, les résultats et les commandes gagnent une grammaire de mouvement sobre ; la progression ne change que sur événement NDJSON et toutes les animations sont neutralisées par `prefers-reduced-motion`. |
| D-030 | Bloquer l’extension « System Exposure » tant que le CLI `aplomb scan` absent n’a pas réussi trois runs | 100 | Aucun Python, Typer, Rich, binaire `aplomb`, dépôt de démonstration ou preuve de run n’existe dans le workspace. Le prompt interdit explicitement de franchir ce gate. |
| D-031 | Ne jamais prétendre scanner Windows depuis le Worker web | 100 | PowerShell, registre, SMB, BitLocker et listeners appartiennent au poste local. Une future collecte doit vivre dans le vrai CLI, avec consentement, puis transmettre uniquement un contrat JSON strict et expurgé. |
| D-032 | Évaluer un listener contre la politique effective du pare-feu, pas contre la seule absence de règle `Block` | 100 | Microsoft documente un blocage entrant par défaut et une précédence de règles. Le profil actif, `DefaultInboundAction`, l’`ActiveStore` et ses filtres doivent être croisés ; sinon le constat reste `heuristic`. |
| D-033 | Générer un PDF persistant uniquement depuis le `report_payload` relu sous la session et la RLS | 100 | Le navigateur n’est pas une source d’autorité : en mode Supabase il n’envoie que l’identifiant. Un rapport absent ou cross-tenant partage le même 404 et le payload stocké est revalidé avant rendu. |
| D-034 | Consommer un quota Postgres atomique avant tout appel OpenAI | 93 | Cinq démarrages par utilisateur et par fenêtre d’une heure bornent le coût tout en laissant des reprises. La RPC verrouille par utilisateur et échoue fermée ; le seuil devra être recalibré avec des données d’usage réelles. |
| D-035 | Rejeter explicitement les montants vides et relier chaque erreur à son champ | 99 | `Number("")` valait zéro et pouvait fausser le statut PME/SMC. Les fixtures prouvent désormais que vide échoue et que zéro explicite reste valide. |
| D-036 | Garder connexion et téléchargement accessibles sur mobile | 97 | La navigation principale disparaît sous 900 px ; des actions dédiées maintiennent le parcours d’authentification et l’accès au ZIP au clavier comme au tactile. |
| D-037 | Publier une notice de traitement exacte sans inventer de durée de conservation | 98 | L’interface nomme les données envoyées à OpenAI et celles persistées avec un compte, puis indique honnêtement que ce MVP privé n’a ni durée ni suppression automatique configurée. |
| D-038 | Ajouter des en-têtes Worker anti-framing et de réduction de surface | 96 | CSP `frame-ancestors`, `X-Frame-Options`, `nosniff`, Referrer-Policy, Permissions-Policy et HSTS sur HTTPS sont appliqués à chaque réponse et couverts par le test HTTP. |
| D-039 | Fournir un ZIP Windows **one shot** pour l’application web locale | 94 | Le lanceur vérifie Node 22.13+, saisit la clé sans écho, exécute `npm ci` et le build, puis sert uniquement `127.0.0.1`. L’archive vient d’une allowlist et exclut dépendances, caches, builds et secrets. |
| D-040 | Ne jamais présenter le ZIP local comme l’audit machine « System Exposure » | 100 | Le téléchargement lance le produit web existant sans élévation ni interrogation OS. L’extension machine reste bloquée par D-030 jusqu’au retour du vrai moteur CLI. |
| D-041 | Forcer PostCSS `8.5.10` sous Next sans changer la version applicative | 90 | `npm audit` signalait la version transitive `8.4.31` (GHSA-qx2v-qp2m-jg93). L’override reste acceptable seulement si installation, audit, build et tests Worker passent ensemble. |
| D-042 | Restreindre l’heuristique d’affichage des pourcentages aux **fractions strictement comprises entre 0 et 1** | 100 | L’ancienne heuristique `0 ≤ v ≤ 1 → ×100` affichait un score ou une confiance de 1/100 comme 100/100 en vert, sur quatre emplacements du rapport. Les scores internes étant des entiers 0-100 (Zod `int().min(0).max(100)`, `computeReadinessScore`), seule une fraction non entière peut être un ratio ; les ratios explicites passent par `ratioToPercentage`. Couvert par `tests/score-display.test.ts`. |
| D-043 | Gater `npm test` sur **lint + typecheck** en tête de chaîne et ajouter une CI GitHub Actions | 97 | Le bug D-042 n’aurait été intercepté par aucun garde-fou automatisé : ni `tsc`, ni `eslint`, ni CI n’étaient exécutés automatiquement. `npm test` enchaîne désormais lint, `tsc --noEmit`, tests unitaires, build et tests HTTP sous Workerd ; `.github/workflows/ci.yml` rejoue la même chaîne sur push et pull request. |
| D-044 | Linter `build/` (plugins Vite actifs) et exclure `public/` et `outputs/` de tsc/eslint | 96 | Le `globalIgnores` hérité du scaffold excluait du lint du code exécuté à chaque build ; à l’inverse, une extraction locale du zip dans `public/downloads/` cassait `tsc`. Les ignores reflètent désormais la réalité du projet et `.gitignore` couvre l’extraction locale. |
| D-045 | Contre-vérifier chaque classification LLM par un **moteur de règles déterministe** (`rules.ts`), mécanisme entièrement nouveau introduit lors de cet audit | 95 | Double lecture auditable : contradictions entre les faits structurés extraits et la classification (art. 5, 6(3), 50(4), GPAI) déclenchent une divergence qui plafonne le score à 59 et exige une revue humaine ; l’analyse lexicale du référentiel produit des alertes non bloquantes. Le résultat est versionné (`preuvance-crosscheck-v1`), journalisé dans le journal des décisions et affiché en bannière. Le LLM ne rend jamais seul le verdict final. |
| D-046 | Dériver toutes les dates réglementaires de la page d’accueil du **référentiel JSON**, jamais de la copie | 98 | La duplication marketing/référentiel pouvait dériver silencieusement à la prochaine mise à jour du droit. `app/lib/marketing.ts` calcule bandeau, échéance hero et pied de page depuis `eu-ai-act-reference.json` ; `tests/marketing-reference.test.ts` verrouille la propagation. |
| D-047 | Servir la page d’accueil en **composant serveur** avec un îlot client `AssessmentExperience` | 93 | Le contenu marketing statique n’a plus besoin d’hydratation ; le formulaire, la progression NDJSON et les résultats (montés par portail dans un emplacement stable) restent client. Le test HTTP vérifie que le HTML serveur contient toujours la page française complète. |
| D-048 | Enrichir le référentiel GPAI (**10^25 FLOP**, art. 53/55, Code de bonnes pratiques) et préciser NCII et art. 50(2) | 97 | Recherche multi-sources du 13/07/2026 : le seuil de risque systémique de l’article 51 manquait au référentiel ; la nouvelle interdiction NCII/CSAM de l’Omnibus signé vise une application au 02/12/2026 sous réserve de publication ; les systèmes mis sur le marché après le 02/08/2026 ne bénéficient d’aucune transition 50(2). L’Omnibus ne modifie ni le seuil ni le fond des articles 51 à 55. |
| D-049 | Comprendre les montants français (espaces, virgule décimale) et rejeter les séparateurs ambigus | 98 | `parseMillionsInput` normalise « 1 234,5 » et refuse « 1,234,567 » plutôt que de le mal interpréter ; la même fonction sert la validation et la soumission. Fixtures dans `tests/assessment-form.test.ts`. |
| D-050 | Supprimer le code mort du scaffold (`chatgpt-auth.ts`, `db/`, `drizzle/`, `examples/`) | 97 | Aucune référence dans le code ni dans l’allowlist de packaging ; la copie conditionnelle de `drizzle/` a ensuite été retirée du plugin Sites (D-051). Réduit l’ambiguïté d’architecture actée en D-026. |
| D-051 | Propager la contre-vérification, le journal des décisions et les raisons des plafonds **jusqu’au PDF** | 98 | Le PDF est l’unique artefact réellement remis à un courtier : le contrat `preuvanceAssessmentSchema` porte désormais `crossCheck`, `decisionLog` et `result.appliedCaps` (optionnels, rétrocompatibles), rendus dans une section dédiée « Journal des décisions et contre-vérification », et le résumé exécutif nomme chaque plafond avec sa raison. La promesse de la page d’accueil (« journal des décisions … dossier partageable ») devient exacte. Test : « un cross-check divergent est propagé jusqu’au contrat PDF ». |
| D-052 | Verrouiller le rendu complet des résultats par un test d’intégration `renderToStaticMarkup` | 96 | Le correctif D-042 n’était couvert qu’en isolation : `tests/assessment-results-render.test.tsx` rend désormais `AssessmentResults` avec le pire cas (1/100 partout, cross-check divergent) et prouve qu’aucun « 100/100 » ni ton vert n’apparaît, que la bannière et la traçabilité s’affichent. Sans nouvelle dépendance de test. |
| D-053 | Structurer le seuil GPAI : champ extrait `trainingComputeAboveGpaiThreshold` + règle déterministe | 94 | Le seuil 10^25 FLOP n’était que narratif. L’extraction ne le remplit que sur déclaration explicite (sinon `null`), et une divergence est levée si le seuil est déclaré dépassé alors que les obligations GPAI sont conclues « non applicables ». La vérification chiffrée ne repose plus uniquement sur le jugement du LLM. |
| D-054 | Resserrer les règles lexicales à des contextes non ambigus | 95 | « Émotions » exige la proximité (≤ 90 caractères) d’un terme travail/enseignement, la biométrie temps réel exige « identification biométrique »/« reconnaissance faciale » : un tableau de bord « temps réel » ou une « classe de produit » ne déclenchent plus d’alerte. Testé sur faux amis bénins. |
| D-055 | Ne plus étiqueter « Documenté » un contrôle simplement **déclaré** | 97 | Constat fondé de l’audit externe ChatGPT 5.6 : un contrôle mentionné dans la description devenait « Documenté » dans le PDF. Statut `declared` ajouté au contrat, libellé « Déclaré · non vérifié », détail « aucune pièce justificative vérifiée ». Chaîne déclaration → pièce → vérification honnête. |
| D-056 | Détecter les **incohérences niveau de risque ↔ pratique interdite / haut risque** | 96 | Autre constat fondé de ChatGPT 5.6 : le schéma acceptait « risque minimal » avec une pratique interdite applicable. Le moteur de contre-vérification lève désormais une divergence (plafond 59, revue humaine) si le `riskTier` contredit une pratique interdite `applies`/`likely_applies` ou une qualification annexe I/III. Tests dédiés. |
| D-057 | Faire du **scan local l’option A** du parcours web | 94 | La page d’accueil présente d’abord « Scanner votre poste en local » (recommandé), puis l’évaluation express en ligne. Nouvelle page `/scan` qui lit le rapport `preuvance-scan.json` **dans le navigateur, sans upload**, et affiche un score d’exposition déterministe. Couvert par le test HTTP. |
| D-058 | Livrer un **scan local PowerShell** sans élévation, avec consentement | 93 | `scripts/preuvance-scan.ps1` : profil personnel/professionnel (`PartOfDomain`/`dsregcmd`), inventaire de fichiers sensibles par nom/extension avec empreinte SHA-256 **sans copie de contenu**, détection « shadow AI » par nom d’hôte (cache DNS + connexions TCP), mode surveillance d’une heure. Fichier `.ps1` encodé UTF-8 **avec BOM** (PowerShell 5.1). Rapport validé par le contrat Zod partagé ; fixture réelle gelée dans les tests. |
| D-059 | Détecter le shadow-AI par **nom d’hôte**, jamais par plage d’IP | 96 | Recherche de sources primaires : les plages IP d’OpenAI (Cloudflare) et Azure OpenAI (Front Door) sont partagées et non fiables — s’y fier créerait des faux positifs. Seul le nom d’hôte résolu (cache DNS) déclenche une détection ; le processus est corrélé au mieux. Les limites (TTL, échantillonnage, résolution ≠ connexion) sont écrites dans le rapport. |
| D-060 | Score d’exposition **déterministe** issu du scan | 94 | `app/lib/assessment/scan-scoring.ts` (`preuvance-scan-scoring-v1`) : un appel d’IA non déclaré est critique et abaisse fortement le score ; secrets exposés, données personnelles et observation réseau limitée pénalisent aussi. Aucun LLM. Tests unitaires + fixture. |
| D-061 | Fournir un **désinstalleur propre** sans élévation | 95 | `scripts/uninstall-preuvance-local.ps1` + `DESINSTALLER_PREUVANCE.cmd` : suppriment d’abord la clé API (`.env.local`), puis caches, dépendances, sorties et rapports de scan ; option « tout supprimer » pour le dossier complet. Testé sur une copie jetable. |
| D-062 | Conserver l’**inventaire/pointage** plutôt que la copie des fichiers sensibles | 98 | Choix explicite de l’utilisateur : ne jamais copier un fichier sensible. Le scan n’enregistre que chemin, taille, dates et empreinte SHA-256. Une note rappelle que le SHA-256 nu est une pseudonymisation (EDPB) et que le rapport reste local ; `-NoHash` retire les empreintes. |
| D-063 | Simplifier le mot-symbole autour du **E à trois barres**, sans animation ni effet décoratif | 97 | La marque reste reconstruite en HTML/CSS pour être nette et accessible, mais les barres deviennent strictement géométriques et immobiles. Une version raster de présentation est conservée séparément ; le produit n’en dépend pas. |
| D-064 | Introduire la **concordance déclaré / observé** : la déclaration d’usage d’IA recueillie avant le scan est corroborée ou contredite par l’observation réseau | 96 | Facteur différenciant identifié par recherche de marché (juillet 2026) : les outils GRC déclarent sans vérifier, les outils shadow-AI observent sans cadre déclaratif, et au moins un assureur IA souscrit sur simple déclaration — personne ne mesure l’écart entre les deux. Esprit de l’art. L113-2 du Code des assurances. Résout la limite documentée « declared toujours false » (Claude Code n’est plus un faux positif critique une fois déclaré). Schéma `declaration` rétrocompatible ; `declared` recalculé côté application à partir du nom d’hôte, jamais sur la seule foi du champ du scanner. Verdicts : concordant / divergent / non corroboré / sans déclaration. Tests unitaires + fixture réelle concordante. |
| D-065 | Doter le script de scan d’**autotests embarqués** (`-SelfTest`) exécutés dans la chaîne standard, et d’un **garde-fou de synchronisation TS ↔ PowerShell** | 97 | 34 assertions sur les fonctions pures (catalogue, catégories de fichiers, adresses privées, analyse de déclaration), lancées par `tests/scan-script.test.ts` (pwsh en CI ubuntu). Le même test échoue si le catalogue PowerShell diverge du contrat TypeScript (identifiants, labels, motifs) — une dérive réelle (« Gemini/Vertex » vs « Gemini / Vertex ») a été corrigée à cette occasion. Dès sa première exécution, l’autotest a révélé un bug de production : `^\.env$` ancré au début de chaîne ne détectait jamais un fichier `.env` nu (la fonction reçoit un chemin complet) ; corrigé en `(^|[\\/])\.env$`. |
| D-066 | Corriger cinq défauts d’exactitude du scoring et du cross-check relevés par le panel de re-notation | 95 | (1) Un lot d’identifiants/certificats n’est plus étiqueté « secret ou clé d’API » : le titre reflète la composition réelle. (2) La catégorie `other` n’est plus silencieusement exclue du score (constat mineur dédié). (3) `insufficient_information` face à un signal structuré extrait est désormais une divergence, comme `does_not_apply` — un classifieur qui ignore une extraction déjà faite ne peut plus passer inaperçu. (4) La formule de pénalité gravité × état × priorité est factorisée (`rawGapPenalty`) au lieu d’être dupliquée. (5) Les casts `as` de `synthesis.ts` sont remplacés par un `Record` typé et `satisfies` ; les gaps sont bornés à 25 côté PDF sans dépendre du plafond amont. Tous couverts par tests. |
| D-067 | Clarifier le parcours non technique : ce que **produit** chaque option, badges traduits, vulgarisation dans le produit, avertissement SmartScreen | 94 | Constat du panel (61/100 en utilisabilité) : l’option A « recommandée » ne produit ni classification ni PDF courtier alors que le hero le promet. Chaque carte affiche désormais « Produit : … » ; la page `/scan` propose un CTA explicite vers l’option B après lecture du rapport ; les badges de sévérité sont en français ; la page `/en-clair` rend la vulgarisation accessible depuis la navigation et le pied de page ; l’avertissement SmartScreen est anticipé avec la marche à suivre. |
| D-068 | Encoder la procédure de vérification du dépôt dans un **skill projet** (`.claude/skills/fable-gate`) | 92 | Les pièges récurrents payés par des incidents réels (BOM UTF-8 des `.ps1`, verrou OneDrive pendant `Compress-Archive`, synchronisation TS/PS1, énumération des sévérités dans les workflows de notation, protocole zéro-faux-positif) sont documentés en procédure exécutable plutôt que dans la seule mémoire de session. |

## Protocole de modification

Pour toute nouvelle décision matérielle :

1. ajouter une ligne `D-xxx` ;
2. indiquer une note entière sur 100 ;
3. citer la preuve ou le test qui la soutient ;
4. préciser ce qui ferait baisser ou réviser la note ;
5. mettre à jour le README si la décision change une promesse utilisateur.

---

Décisions D-001 à D-041 : rédaction initiale du projet. Décisions D-042 à D-062 : audit, corrections, scan local et désinstalleur des 13-14 juillet 2026. Décisions D-064 à D-068 : concordance déclaré / observé, autotests du scan et corrections d'exactitude des 15-16 juillet 2026. Rédigées par **Fable (Claude Fable 5), Anthropic**. Les décisions D-055 et D-056 répondent à des constats fondés d'un audit externe produit par **ChatGPT 5.6** (voir [`docs/revue-audit-externe.md`](docs/revue-audit-externe.md)) ; l'attribution de cet audit à son auteur est conservée, et le présent registre n'est pas signé sous une autre identité que celle qui l'a rédigé.
